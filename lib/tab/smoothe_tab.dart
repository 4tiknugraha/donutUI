import 'package:flutter/material.dart';

class SmootheTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Smoothe Tabs'),
      ),
    );
  }
}
